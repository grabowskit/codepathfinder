apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: index-data-pvc
  namespace: code-pathfinder
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard-rwo
